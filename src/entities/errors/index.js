const { invalidEmailError } = require('./invalid-email-error');
const { invalidPasswordError } = require('./invalid-password-error');
const { invalidTitleError } = require('./invalid-title-error');

module.exports = {
  invalidEmailError,
  invalidPasswordError,
  invalidTitleError,
};
